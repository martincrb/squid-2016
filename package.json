{
  "name": "squid-2016",
  "version": "0.0.1",
  "description": "Game where squids shoot each other!",
  "main": "src/server.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "browserify src/client.js -o dist/bundle.js && (echo 'go es!' || echo 'built!')",
    "test": "tape test/**/*.spec.js | tap-spec"
  },
  "author": "Mart�in Cristobal",
  "license": "ISC",
  "devDependencies": {
    "standard": "^7.1.2",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
